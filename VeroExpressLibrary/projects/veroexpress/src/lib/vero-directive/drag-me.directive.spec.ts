import { DragMeDirective } from './drag-me.directive';

describe('DragMeDirective', () => {
  it('should create an instance', () => {
    const directive = new DragMeDirective();
    expect(directive).toBeTruthy();
  });
});
