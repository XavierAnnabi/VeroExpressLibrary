import { ResizeMeDirective } from './resize-me.directive';

describe('ResizeMeDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeMeDirective();
    expect(directive).toBeTruthy();
  });
});
